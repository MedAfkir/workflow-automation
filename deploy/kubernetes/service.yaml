apiVersion: v1
kind: Service
metadata:
  name: workflow-app-api
  namespace: workflow
  labels:
    app.kubernetes.io/name: workflow-platform
    app.kubernetes.io/component: app-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: workflow-platform
    app.kubernetes.io/component: app-api
